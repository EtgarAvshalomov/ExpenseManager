const Loading = () => {
    return (
        <div className="centered" style={{height: '80vh'}}>
        <p style={{fontSize: '18px'}}>Loading...</p>
        </div> // Show this until data is fetched
    );
}

export default Loading;